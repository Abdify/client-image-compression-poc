{
  "name": "image-compression-experiment",
  "version": "1.0.0",
  "description": "A web-based tool to experiment with and compare different image compression techniques",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p 3000 -o",
    "dev": "http-server -p 3000 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "browser-image-compression": "^2.0.2",
    "compressorjs": "^1.2.1",
    "img-comparison-slider": "^8.0.6",
    "js-image-zoom": "^0.7.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
